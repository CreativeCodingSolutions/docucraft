{
  "name": "docucraft-webhook",
  "version": "1.0.0",
  "private": true,
  "description": "DocuCraft GitHub App webhook server — auto-generates PR descriptions with one-click install",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.2.0",
    "@octokit/request": "^9.2.0",
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
